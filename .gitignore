dist
.wrangler
.output
.vercel
.netlify
.vinxi
app.config.timestamp_*.js

# Environment
.env
.env*.local

# dependencies
/node_modules

# IDEs and editors
/.idea
.project
.classpath
*.launch
.settings/

# Temp
gitignore

# System Files
.DS_Store
Thumbs.db

# Pipeline artefacts
pipeline/pages/
# Sans barre finale, volontairement. `pipeline/handoffs/` ne matche qu'un
# repertoire, or ce repertoire est absent d'un checkout neuf puisqu'il est
# justement ignore. `git check-ignore` repondait alors « non ignore », et
# `apply-profile --check` echouait en CI en passant en local.
pipeline/handoffs
pipeline/runs/
test-results/
playwright-report/
coverage/
